---
title: A2팀 화면별 옵션값 확정
author: 손찬용 PM
category: 01_기획
status: draft
created: 2026-07-07
updated: 2026-07-20
version: v0.12
document_group: spec
tags:
  - A2팀
  - IA
  - 옵션값
  - 와이어프레임
summary_target: false
---

# A2팀 화면별 옵션값 확정
> 작성일: 2026-07-07 | 갱신: 2026-07-20 | 작성자: 손찬용 PM
> 목적: 와이어프레임에 들어갈 옵션값을 디자이너·BE가 한 곳에서 확인할 수 있도록 정리
> 상태: 아래 "8. 미해결 항목"만 남으면 v1.0(확정본)으로 승격

## 0. 전체 디자인 방향

Higgsfield를 레퍼런스로 따라가는 방향으로 진행. 상세 보기·공유 화면 포맷(6절)도 Higgsfield 예시 그대로 참고.

## 1. 홈 화면

| 항목 | 값 |
| --- | --- |
| 히어로 메시지 | 헤드라인: "방송 제작에 필요한 이미지와 영상, AI로 빠르게" / 서브텍스트: "캐릭터·배경·영상까지, 아이디어를 몇 분 안에 완성된 비주얼로" (1.1절 A안 확정) |
| 입력창 | 프롬프트 입력 → 칩(초기 레퍼런스 / 키이미지 / 영상) 선택 → 엔터 → 해당 메뉴로 이동, 프롬프트 자동 채움. 칩 미선택 시 기본값은 초기 레퍼런스(메뉴 A). 순수 이동(라우팅) 용도이며 여기서 직접 생성하지 않음 |
| 전체 구성 | 대시보드 중심. 좌측 사이드바 여부는 별도 논의 중 |

### 1.1 히어로 카피 후보

입력창이 이미 "어떤 이미지나 영상을 만들어볼까요?"로 액션을 유도하므로, 히어로 카피는 그거랑 겹치지 않게 **한 단계 위에서 가치 제안**을 하는 역할로 잡았다.

| 후보 | 헤드라인 | 서브텍스트 |
| --- | --- | --- |
| A. 베네핏 중심 | 방송 제작에 필요한 이미지와 영상, AI로 빠르게 | 캐릭터·배경·영상까지, 아이디어를 몇 분 안에 완성된 비주얼로 |
| B. 전문성 강조 | 제작진을 위한 AI 비주얼 제작 도구 | 방송·영상 콘텐츠에 바로 쓸 수 있는 이미지와 영상을 생성하세요 |
| C. 간결형 | 상상한 장면을 지금 만들어보세요 | 캐릭터, 배경, 영상 생성까지 한 곳에서 |

**확정: A안.** "방송 제작"이라는 실제 타깃 맥락을 헤드라인에 바로 넣어서 서비스 정체성이 분명하고, 서브텍스트가 캐릭터/배경/영상이라는 3대 기능을 자연스럽게 소개함.

## 2. 이미지 생성 — 메뉴 A(초기 레퍼런스)

| 옵션 | 값 |
| --- | --- |
| 모델 | GPT Image 2 (고정, 선택 UI 불필요) |
| 목적 | 캐릭터 / 배경 — 복수 선택 가능. 캐릭터만 선택 시 캐릭터 1장, 배경만 선택 시 배경 1장, 둘 다 선택 시 캐릭터 1장 + 배경 1장 출력 (별도 수량 옵션 없음) |
| 품질 | 표준(medium, 기본) / 고화질(high, 옵션) |
| 비율 | Auto(기본) / 1:1 / 16:9 / 9:16 |
| 레퍼런스 첨부 | Style + Character + Add, 최대 8장, PNG/WEBP/JPG 50MB 이하 |
| 프롬프트 향상(구 "AI 프롬프트 교정") | ON/OFF 토글. ON 시 Claude Sonnet이 변환한 GPT Image 2 최적화 프롬프트(한국어 입력 → 영어)를 내부 생성 요청에 사용. **변환된 영어 프롬프트는 사용자 화면에 노출하지 않음(2026-07-14 확정)**. 이름을 바꾼 이유는 2.1절 참고 |
| 캐릭터 다각도 출력 | 정면·측면·후면을 1장에 담아 출력. 프롬프트 엔지니어링으로 처리(BE 별도 작업 불필요) |

### 2.1 "AI 프롬프트 교정" → "프롬프트 향상" 개명 배경

기존 이름의 문제는 "교정"이 단순 오탈자 수정 정도로 읽혀서, 실제로 한국어가 영어로 통째로 바뀌는 걸 사용자가 예상 못 한다는 것. 검토한 후보: 한국어 프롬프트 변환, 한국어로 쓰기, AI 번역·최적화, 스마트 프롬프트, 매직 프롬프트.

**확정: "프롬프트 향상"** — Leonardo AI(Prompt Enhance), Adobe Firefly(Enhance prompt option) 등 주요 AI 생성 툴들이 실제로 쓰는 "Enhance" 계열 네이밍의 한국어화. "번역"처럼 특정 동작 하나만 예고하는 게 아니라 "결과물이 더 좋아진다"는 포괄적 기대를 심어줘서, 언어가 바뀌든 디테일이 추가되든 사용자가 자연스럽게 받아들인다.

## 3. 이미지 생성 — 메뉴 B(키이미지)

| 옵션 | 값 |
| --- | --- |
| 모델 | GPT Image 2 (고정) |
| 목적 | 키이미지 캐릭터시트 (표시만, 메뉴 진입으로 확정) |
| 품질 | 표준(medium) / 고화질(high) |
| 비율 | Auto / 1:1 / 16:9 / 9:16 |
| 수량 | 1장 고정, UI 미노출 |
| 입력 방식 | 속성 선택 사이드바 + 자유 텍스트 프롬프트 입력창, 둘 다 제공 |
| 속성 선택 사이드바 | 파트1 캐릭터 기본(성별·나이대·체형) / 파트2 아트 스타일·세계관(세계관 중 "역사"는 중세·서양풍, "무협·동양"은 동양 사극·시대극 계열) / 파트3 헤어 / 파트4 얼굴 / 파트5 의상·악세서리. 전체 선택형 — 모든 항목에 기본값이 미리 채워져 있고 사용자는 값을 바꾸기만 함 |
| 복수 선택 항목 | 악세서리만 복수 선택 가능(예: 안경+목걸이 동시 착용), 나머지 속성은 전부 단일 선택 |
| 컬러 항목(헤어색상·눈색상·의상색상) | 프리셋 라벨이 아닌 컬러 팔레트(스와치) + 헥스코드 직접 입력, 둘 다 제공 |
| 기본값 고정(UI 미노출) | 포즈: 정면+측면+후면 / 배경: 흰 배경 / 수량: 1장 |
| 프롬프트 향상(구 "AI 프롬프트 교정") | ON/OFF. ON 시 Claude Sonnet이 변환한 최적화 프롬프트를 내부 생성 요청에 사용. **변환 결과는 사용자 화면에 노출하지 않음(2026-07-14 확정)**. 이름을 바꾼 이유는 2.1절 참고 |
| 메뉴 A 결과물과의 연동 | 이번 범위 제외 — 사용자가 처음부터 속성을 직접 선택(자동 연동은 후속안건으로 별도 확장) |
| 캐릭터 범위 | 인간형 한정(성별: 남성/여성/젠더리스). 동물형·로봇형·수인형은 이번 범위 밖 |

## 4. 영상 생성 화면

| 옵션 | 값 |
| --- | --- |
| 모델 | Seedance 2.0 / Kling 3.0 — 카드 형태로 노출. **둘 다 스토리보드 영상 생성 용도(2026-07-13 확정)**, 역할 분담 없음 (기존 "데모용/테스트용" 포지셔닝은 폐기) |
| 길이 | Kling 3.0: 3~15초 / Seedance 2.0: 4~15초 — 모델별 최소값이 다름. 선택 모델이 지원 안 하는 값은 UI에서 비활성화 처리 |
| 비율 | Seedance: `auto, 21:9, 16:9, 4:3, 1:1, 3:4, 9:16`(전부 지원) / Kling: `16:9, 9:16, 1:1`. 지원 안 하는 값은 UI에서 비활성화 처리 |
| 해상도 | Seedance 2.0: 해상도를 사용자가 직접 선택(720p·1080p, 4K는 미확인). Kling 3.0: v1에서는 Standard로 고정(1080p), 해상도 필드는 읽기 전용 표시 — 이유는 4.2절 참고 |
| Bitrate | High 고정, UI 미노출. BE 환경변수(demo/prod/showcase)로 제어. Kling은 Bitrate 선택지 자체가 없어 해당 없음 |
| 스토리보드 입력 | **9컷 3×3 그리드 이미지 1장 + 프롬프트 1건** 업로드. 그리드는 B서비스에서 실시간으로 전달받는 것이 아니라 **예시로 수령한 이미지 또는 자체 생성분**을 사용한다(2026-07-13 정정). 컷 순서는 좌상단=1번 → 우하단=9번이며 이 순서대로 scene/shot이 전개되고, 프롬프트는 변환 없이 그대로 생성에 투입한다. 자체 생성 시 컷 번호 오버레이가 영상에 새겨질 수 있으므로 **영상 입력용은 번호 없는 버전으로 생성**한다. 모델별 전달 방식은 4.3절 참고 |
| 프롬프트 향상 | **영상 화면에는 미적용 확정(2026-07-13)** — 07.07 합의(이미지 생성에만 적용) 및 B서비스 프롬프트 무변환 투입 전제에 따라 토글 없음 |

### 4.1 영상 생성 단가표 (fal.ai 공식 기준, 15초 영상 1개 기준)

| 모델 | 해상도 | 초당 단가 | 15초 영상 1개 |
| --- | --- | --- | --- |
| Kling 3.0 Standard | 1080p | $0.126/초 (`generate_audio: true` 확정 반영) | $1.89 (약 2,600원) |
| Kling 3.0 Pro | 1080p | $0.112/초 | $1.68 (약 2,300원) |
| Kling 3.0 | 4K | $0.42/초 | $6.30 (약 8,700원) |
| Seedance 2.0 Mini | 480p | $0.043/초 | $0.65 (약 900원) |
| Seedance 2.0 | 720p | $0.3034/초 | $4.55 (약 6,300원) |
| Seedance 2.0 | 1080p | $0.682/초 | $10.23 (약 14,000원) |
| Seedance 2.0 | 4K | 비공개(Early Access, 별도 승인 필요 — 현재 일반 사용 불가) | — |

**Seedance는 720p→1080p로 한 단계만 올려도 비용이 2배 넘게 뜁니다.** Seedance 1080p($10.23/15초)는 Kling 4K($6.30)보다도 비쌉니다. 이 표가 아래 일일 생성 한도를 모델·해상도별로 나눈 이유입니다.

### 4.2 Kling 해상도 선택 UI 초안

문제: Seedance는 해상도가 한 모델 안에서 자유 선택 파라미터지만, Kling은 Standard/Pro/4K가 사실상 **서로 다른 모델**이라 해상도가 선택 파라미터가 아니라 모델 자체에 고정돼 있음. 그래서 Seedance와 같은 "해상도 드롭다운" UI를 그대로 못 씀.

**진행안**: v1(이번 스프린트)에서는 **Kling을 Standard 하나로 고정**하고 Pro·4K는 노출하지 않는다. 이렇게 하면 UI가 단순해진다(Kling 선택 시 해상도 필드는 "1080p 고정"으로 읽기 전용 표시, Seedance 선택 시에만 해상도 드롭다운 활성화). 화면 라벨은 티어명 없이 "Kling 3.0"으로만 노출하고, 티어는 BE 엔드포인트 선택의 문제로 둔다. Kling Pro·4K 노출(MVP 제출본 고사양 구성 의견 포함)은 필요해지면 후속으로 별도 설계.

### 4.3 스토리보드 입력(3×3 그리드 + 프롬프트)의 모델별 전달 방식 — fal.ai API 확인 결과 (2026-07-13)

입력은 ① 9컷 3×3 그리드 이미지 1장(예시 수령분 또는 자체 생성분), ② 컷 순서(1=좌상단 → 9=우하단)대로 scene/shot을 서술한 프롬프트 1건이며, 프롬프트는 그대로 투입하는 것이 전제다. **Seedance·Kling 모두 스토리보드 영상 생성 용도로 확정(2026-07-13)** — 역할 분담 없이 두 모델 다 같은 입력을 받는다. I2V 모델은 입력 이미지를 첫 프레임 그 자체로 쓰는 구조라 그리드 1장을 통째로 넣으면 그리드가 화면에 그대로 노출되므로, **어느 모델이든 BE에서 그리드를 9장으로 슬라이스(단순 crop)하는 전처리가 전제**다(이슬기 PM 조사, 2026-07-13). 모델별 전달 방식은 아래와 같다.

**Seedance 2.0 — `bytedance/seedance-2.0/reference-to-video` (9장 분리 입력, 확정 방향)**

- 슬라이스한 9장을 순서대로 레퍼런스 입력(최대 이미지 9장 + 영상 3개 + 오디오 3개 지원), 프롬프트에서 `@Image1`~`@Image9`로 컷을 참조. 멀티샷 컷 전환(15초 내)을 네이티브 지원해 9컷 스토리보드 구조와 정확히 맞는다.
- 프롬프트를 `@ImageN` 참조 형식으로 변환하는 전처리 필요(규칙 기반 치환 가능한지 BE 확인 중).
- 오디오: `generate_audio` 기본 ON, OFF 선택 가능. **오디오 여부와 무관하게 요금 동일**(fal 공식 문서 명시).
- `fast/` 티어(저비용)도 별도 존재.

**Kling 3.0 Standard — `fal-ai/kling-video/v3/standard/image-to-video` (시작/끝 프레임 + 그리드 레퍼런스, BE 현행 방식)**

- 이미지 9장 동시 입력이 불가한 엔드포인트(레퍼런스 슬롯 최대 4장 — BE 확인, 2026-07-13). BE 현행 방식: **1번 컷 = `start_image_url`(시작 프레임), 9번 컷 = `end_image_url`(끝 프레임), 3×3 그리드 전체를 레퍼런스로 추가** 입력해 중간 컷의 시각 정보를 보완. 시작·끝은 보장되지만 중간 2~8번 컷 준수 수준은 비교 테스트로 검증.
- 컷N→컷N+1 세그먼트 체이닝(ffmpeg concat) 방식은 순서 100% 보장되나 최소 3초 × 8세그먼트 = 24초 과금 + 이음새 리스크로 **제외**.
- `multi_prompt`(샷별 prompt+duration)는 최대 6샷 + 총 15초 제한이라 9컷과 1:1 매핑 불가.
- `generate_audio` **기본값 true로 확정(2026-07-20)** — 소리 포함, UI 토글 미노출. 요금 ON $0.126/초(+50%, OFF 대비) 기준으로 4.1절 단가표 반영 완료.
- 비율·해상도 입력 파라미터 없음(Standard 1080p 고정). 출력 비율이 입력 이미지 비율을 따르는지 BE 검증 필요.

**BE 비교 테스트(2026-07-13 확정 시나리오)**: 같은 스토리보드(번호 오버레이 없는 그리드)로 3건, 전부 **15초·720p** (4초에 9컷은 컷당 0.4초라 조건상 불가 — 컷당 1.5~2초 확보가 전제).

| # | 내용 | 예상 비용 |
| --- | --- | --- |
| 1 | Seedance R2V — 3×3 그리드 **1장 그대로** 입력 (슬라이스 불필요 가설 검증) | 약 $4.6 |
| 2 | Seedance R2V — **9장 슬라이스** 입력 | 약 $4.6 |
| 3 | Kling 현행 방식(시작 컷1 + 끝 컷9 + 그리드 레퍼런스) | 약 $1.3 |

판정: 순서 유지 / 컷 식별 개수 / 전환 자연스러움 3항목 비교. 갈림길 — 1번이 2번만큼 좋으면 슬라이스 개발 불필요로 확정, 2번이 확실히 좋으면 슬라이스 전처리 개발, 3번이 우세하면 Kling 방식 기본 검토. 매 건 생성 소요 시간 기록(진행 표시 UI 판단용).

## 5. 내 프로필 화면

| 항목 | 값 |
| --- | --- |
| 구성(최종 IA v2.1 기준) | 프로필(닉네임·사진) / Google 로그인 및 보안(비밀번호 변경) / 활동(가입일 등) / 계정 삭제(탈퇴) |
| 제외 확정 | **구독 상태·플랜은 제외**(2026-07-13, 기능명세서 v0.2 기준 — 충돌 시 최종 IA를 따르기로 함) |

이 절의 기존 제안(계정 정보/구독 상태/사전 프롬프트 가이드/문의/기본 생성 옵션/로그아웃)은 최종 IA v2.1로 대체됐다. 사전 프롬프트 가이드의 진입점(프로필 내부 vs 생성 화면 내부)은 미정으로 남아 있으며, 세부 카테고리 라벨은 디자이너 확인 후 확정한다(미해결 #4).

## 6. 공유하기 / 상세 보기 화면

| 항목 | 값 |
| --- | --- |
| 참고 레퍼런스 | Higgsfield — 상세 패널에 Feature(모델명) / Quality(해상도) / Bitrate / Size(해상도 픽셀값) / Created(생성일) 표시, 하단에 Recreate·Reference·Download·Like·Share 액션 버튼. 이 포맷을 그대로 따라갈 예정 |
| 진입 경로 | 공유 링크로 들어오면 Higgsfield처럼 해당 서비스로 진입하는 방식 참고 |

## 7. 반응형(모바일)

큰 이슈 없음 — FE에서 화면 스탑포인트만 정하면 되는 수준으로 판단.

## 8. 미해결 항목 (v1.0 승격 전 처리)

| # | 항목 | 필요한 조치 | 담당 |
| --- | --- | --- | --- |
| 1 | 이미지·영상 일일 생성 한도 | 값(이미지 30장/일, Kling 10개/일, Seedance 720p 이하 5개/일, Seedance 1080p 2개/일)은 확정. **UI에는 노출하지 않고 BE에서 조용히 제한** — 한도 도달 시에만 안내 메시지("오늘 생성 가능 횟수를 초과했습니다") 표시. 요금제성 기능이 아니라 fal.ai 자동 차단 부재에 대한 안전장치일 뿐이라 카운터 UI로 두면 오해 소지 | BE |
| 2 | Claude Sonnet 프롬프트 변환 레이어 실제 연동 시점 | BE 개발 일정 검토 결과 대기 | BE |
| 3 | Seedance 2.0 4K 해상도 실제 지원 여부 | BE 재테스트. 최종 시연에 쓸 수 있는지가 관건 — 안 되면 1080p로 대체 | BE |
| 4 | ~~내 프로필 화면 카테고리~~ | **해소(2026-07-13)** — 최종 IA v2.1 구성(프로필/Google 로그인·보안/활동/계정 삭제, 구독 제외) 기준으로 디자이너 확인 완료 | ~~디자이너~~ |
| 5 | 백엔드 구현 항목 확인 | 프로필 관련 기능, 영상 처리 기능(진행률 표시 등 기존 미해결 포함) 범위 정리 | BE |
| 6 | ~~오디오 정책 확정~~ | **해소(2026-07-20)** — 두 모델 모두 `generate_audio: true` 고정, UI 토글 미노출으로 팀 자체 확정(PD 공식 확인 없이 진행). 근거: PD 구두 조건("생성 영상에 기본적으로 소리 포함") + Seedance는 요금 동일, Kling은 +50%($0.126/초)이나 절대액 미미. 4.1절 단가표 Kling ON 기준으로 재계산 완료 | ~~PM(PD 확인)·BE~~ |
| 7 | Kling image-to-video 출력 비율 검증 | 비율 파라미터가 없어 입력 이미지 비율을 따르는지 테스트 필요. 결과에 따라 Kling 비율 선택 UI 제거 여부 결정 (4.3절) | BE |
| 8 | 스토리보드 생성 방식 최종 검증 | 15초·720p 3건 비교(그리드 1장 R2V / 9장 슬라이스 R2V / Kling 현행 방식) — 4.3절 확정 시나리오 참고. 결과에 따라 슬라이스 전처리 개발 여부 결정 | PM·BE |
| 9 | ~~영상 생성 진행률 대체 표시~~ | **해소(2026-07-13)** — 퍼센트 추적 불가에 따라 **스피너 방식으로 확정**(PM·디자이너 협의 완료). 와이어프레임 반영 및 FE 통보만 남음 | ~~PM·디자이너·FE~~ |

## 9. 전달 방식

- 위 미해결 항목이 정리되면 v1.0으로 승격하고, 와이어프레임 링크와 함께 Discord로 전달한다.
- BE에게는 API 요청 파라미터(enum 값) 확정 시점에 별도로 공유한다.
